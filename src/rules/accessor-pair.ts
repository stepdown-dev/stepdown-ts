// Rule: accessor-pair
// Paired `get` and `set` for the same property name stay adjacent.
// Purely syntactic — no body-shape analysis. TS-specific (no Go analog).
// Per ADR-0001 (forthcoming).

export const RULE_NAME = "accessor-pair";

// TODO: implement once ADR-0001 + Principles doc land.
