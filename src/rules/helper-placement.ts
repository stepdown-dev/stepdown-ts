// Rule: helper-placement
// A private helper called by multiple exported roots is placed by the first
// exported root (in source order) that calls it.
// Per ADR-0001 (forthcoming).

export const RULE_NAME = "helper-placement";

// TODO: implement once ADR-0001 + Principles doc land.
